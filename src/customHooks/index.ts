/**
 * Custom Hooks Barrel Export
 *
 * This file centralizes all custom hooks exports for easier importing.
 * Import hooks like: import { useResponsive } from '@/customHooks'
 */

// Responsive hooks
export { useResponsive } from "./responsive";

// Re-export types if any are defined in the future
export type * from "./responsive";